#include "GLStar.h"

void Star::updatePhysics(float dt, int timecount, bool real){
	//if (timecount % 800 == 0) {
	//	CULog("at %d, x position is %f", timecount, this->getPosition().x);
	//}
	//if ((timecount / 512) % 2 == 1) {
	//	if (real) {
	//		// update real body
	//		this->getRealBody()->ApplyForceToCenter(b2Vec2(0, 20), true);
	//	}
	//	else {
	//		// update draw body
	//		this->getDrawBody()->ApplyForceToCenter(b2Vec2(0, 20), true);
	//	}
	//}
	
}
